[project]
name = "gateways"
version = "0.1.0"
requires-python = ">=3.14"

[tool.ruff]
line-length = 88
target-version = "py312"

[tool.mypy]
python_version = "3.14"
files = ["opengates"]
explicit_package_bases = true
ignore_missing_imports = true
